const nomes = ['Ana', 'Ju', 'Leo', 'Paula'];

nomes.forEach(imprimeNomes); // Repare na sintaxe, a função imprimeNomes não possui parâmetros na chamada

function imprimeNomes(nome) {
    console.log(nome);
}